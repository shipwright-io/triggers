apiVersion: v1
kind: Service
metadata:
  labels:
    shipwright.io/component: triggers
  name: controller-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: https
  selector:
    shipwright.io/component: triggers
